---
const hasCodeSlot = Astro.slots.has("code");
const hasPreviewSlot = Astro.slots.has("preview");
---

<div
  class:list={["border-divider grid border-(length:--border-width-boundary)"]}
  data-testid="code-preview"
>
  {
    hasCodeSlot && (
      <div class="overflow-x-auto" tabindex="0">
        <slot name="code" />
      </div>
    )
  }

  {
    hasPreviewSlot && (
      <figure
        class:list={[
          "bg-container flex flex-col gap-1 p-4",
          hasCodeSlot &&
            "border-divider border-t-(length:--border-width-divider)",
        ]}
      >
        <figcaption class="text-sm font-semibold">Preview</figcaption>
        <div
          class="border-divider overflow-x-auto border-(length:--border-width-divider) bg-white p-4"
          tabindex="0"
        >
          <slot name="preview" />
        </div>
      </figure>
    )
  }
</div>
