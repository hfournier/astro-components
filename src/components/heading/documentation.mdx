---
name: Heading
description: A heading element that renders as any level `h1`–`h6` HTML tag, styled so a page's own heading hierarchy stays visually consistent.
role: pattern
meta:
  title: Heading | astro-components HenriFournier.dev
  description: A heading element rendering as h1-h6, styled from this repo's Document heading font-size/weight scale for a consistent page hierarchy.
extends:
  kind: element
  tag: [h1]
props:
  - name: level
    type: '"h1" | "h2" | "h3" | "h4" | "h5" | "h6"'
    required: true
    description: Which heading tag to render, and which step of the Document heading scale to style it with (h1 the largest, h6 the smallest).
slots:
  - name: default
    description: The heading's text content.
    kind: any
---
