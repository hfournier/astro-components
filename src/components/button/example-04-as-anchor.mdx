---
title: Button as an anchor
description: Use the `as` property to render Button as an `<a>` instead of a `<button>`, for a link that should look like a button. It accepts `href` and other anchor attributes, styled identically to the button.
---

<Button as="a" href="#">
  Anchor Button
</Button>
