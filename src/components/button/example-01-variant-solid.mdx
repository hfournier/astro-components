---
title: Button variant - solid
description: You can change the appearance of the button by using the `variant` property. The `solid` variant renders a filled button. The `variant` property works in conjuction with the `color` property.
---

<div class="flex flex-wrap gap-2">
    <Button variant="solid" color="primary">Solid Primary</Button>
    <Button variant="solid" color="secondary">Solid Secondary</Button>
</div>
