---
title: Button variant - outline
description: You can change the appearance of the button by using the `variant` property. The `outline` variant renders a bordered button. The `variant` property works in conjuction with the `color` property.
---

<div class="flex flex-wrap gap-2">
    <Button variant="outline" color="primary">Outline Primary</Button>
    <Button variant="outline" color="secondary">Outline Secondary</Button>
</div>
